//! `hpds` — unified tooling for the Stanford HPDS lab.
//!
//! Thin entry point: parse the CLI, dispatch, render top-level errors.
//! The clap command tree lands with the CLI skeleton (spec §2, M0.2); until
//! then this is a deliberate no-op stub so the crate builds and tests run.

fn main() {}
