//! `hpds install` — install external software per platform (spec §7).

/// Stub until M4 lands the installer framework.
pub fn run() -> anyhow::Result<()> {
    Err(super::not_yet_implemented("install"))
}
